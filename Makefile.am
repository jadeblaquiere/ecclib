ACLOCAL_AMFLAGS=-I m4
if COND_BENCHMARKS
  MAYBE_BENCHMARKS = benchmark
endif
if COND_EXAMPLES
  MAYBE_EXAMPLES = examples
endif
if COND_UNITTESTS
  MAYBE_TESTS = tests
endif
SUBDIRS=src include $(MAYBE_TESTS) $(MAYBE_BENCHMARKS) $(MAYBE_EXAMPLES)
